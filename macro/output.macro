	ofstream of ("likel.dat",ios::out)	;

	if (!of)
	{
		cout << "Could not open file. Quitting program." << endl;
		exit(1);
	}
	
	for (int i=0; i < n; i++)
	{
		of <<setw(10) <<  right << setprecision(6) << fixed << x.at(i) << setw(20) << right << math.y.at(i) << endl;
	}

	of.close();
